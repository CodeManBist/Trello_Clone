{
  "version": 2,
  "bunVersion": "1.x",
  "buildCommand": "cd ../.. && bun install && cd packages/db && bunx prisma generate && cd ../../apps/backend && bun run build",
  "functions": {
    "dist/index.js": {
      "runtime": "@vercel/node"
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/dist/index.js"
    }
  ]
}